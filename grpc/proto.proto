syntax = "proto3";
option go_package = "ChitChat/grpc/proto";

message Clients {
    repeated string clients = 1;
}

message Empty {}

service ChitChat {
    rpc GetClients (Empty) returns (Clients) {};
}

